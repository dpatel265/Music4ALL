import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFF6B00); // SimpMusic Orange
  static const Color accent = Color(
    0xFF256AF4,
  ); // Blue accent from previous design, or complementary
  static const Color background = Color(0xFF111318); // Dark background
  static const Color surface = Color(0xFF181B22); // Slightly lighter surface
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA6BA);
  static const Color error = Color(0xFFFF3B30);
  static const Color success = Color(0xFF34C759);
}
